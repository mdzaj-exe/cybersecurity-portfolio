# Start with a base image that includes Java runtime
FROM openjdk:17-jdk-alpine

# Set the working directory inside the container
WORKDIR /app

# Copy the JAR file generated by Maven to the target directory within the container
COPY target/ComputerShop.jar /app/target/ComputerShop.jar

# Expose the port the Spring Boot application will run on
EXPOSE $PORT

# Set the entry point to run the Spring Boot application when the container starts, and bind to the $PORT
ENTRYPOINT ["sh", "-c", "java -jar /app/target/ComputerShop.jar --server.port=$PORT"]
